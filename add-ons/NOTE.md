These add-on driver and filesystem source trees are not used by the current build.

They have been preserved under `uts/add-ons` for future porting into the Python-based build system and kernel source layout.