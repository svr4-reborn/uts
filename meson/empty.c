/* Empty translation unit: an empty Driver.o placeholder for a configured-in,
   non-NODRIVE module that only supplies space.c (e.g. gendisp). idconfig requires
   a Driver.o to exist; this provides one with no symbols. See gen_groups.py. */
