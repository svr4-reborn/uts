# idmkunix.conf -- variable compile/link settings for idmkunix/idbuild.
# GENERATED by uts/meson from idmkunix.conf.in; do not edit on the source side.
#
# Read by idmkunix (default <root>/etc/conf/cf.d/idmkunix.conf) so the build-
# time relink and the on-system `idbuild` reconfigure use the same flags.
# Explicit idmkunix command-line flags still override these.
CC=@CC@
LD=@LD@
CFLAGS=@CFLAGS@
DEFINES=@DEFINES@
INCDIRS=@INCDIRS@
